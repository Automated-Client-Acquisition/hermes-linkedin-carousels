# aca / references

Reference images for the `aca` style. Empty for now — the first carousel
run using this style will use prompt-only anchoring. Once a master
slide-01 in this style is generated and approved, commit it here as
`cover.png` and update `../style.md` to reference it.

Suggested files (add when available):

- `cover.png` — master slide-1 in this style. Used as `--reference` for
  every slide of every future run that picks `--style aca`.
- `body-diagram.png` — a representative body slide (comparison cards or
  3-stage flow) for body-slide anchoring if cover.png doesn't transfer
  cleanly.
- `cta.png` — a representative CTA slide.
